# OFFICE_PERIMETER — alias

> **Deprecated name.** Use
> [`PERIMETER.md`](PERIMETER.md). Copy that file to the city
> root as `PERIMETER.md`.
>
> `found` / `doctor` and the Office lens still accept this filename if present
> so older cities resolve. Prefer `PERIMETER.md` for new plants.
>
> The map WIDTH LAW **spec** under `docs/specs/CITY_EDGES.md` is a separate
> document (grant model prose). Runtime edges live at city-root `PERIMETER.md`.

See [PERIMETER.md](PERIMETER.md).
